import { defineConfig } from 'astro/config';

// Set `site` to your final URL before deploying (used for canonical URLs + sitemap).
// For GitHub Pages project hosting, also set `base: '/<repo-name>'`.
export default defineConfig({
  site: 'https://jasonlayel.com',
});
